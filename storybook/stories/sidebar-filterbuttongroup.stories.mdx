import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { action } from "@storybook/addon-actions";
import { withKnobs, text, object } from "@storybook/addon-knobs";
import Centered from "../components/Centered";

import { FilterButtonGroup } from "@rawpixel-public/react-components";

import "../.storybook/global.css";

<Meta title="Sidebar/Filter Buttons" decorators={[withKnobs]} />

# Filter Button Group

Group of filter buttons to be used in a sidebar component.

<Preview>
  <Story name="default">
    <Centered style={{width: "270px"}}>
      <FilterButtonGroup
        title={text("title", "File Type")}
        filters={object("filters",
          [
            {
              "name": "JPG / TIFF",
              "tag": "$photos",
              "published": true
            },
            {
              "name": "Vectors",
              "tag": "$vectors",
              "published": true
            },
            {
              "name": "PSD",
              "tag": "$psd",
              "published": true
            },
            {
              "name": "PNG",
              "tag": "$png",
              "published": true
            },
            {
              "name": "SVG",
              "tag": "$svg",
              "published": true
            }
          ]
        )}
        onFilterClick={action('filter-click')}
      />
    </Centered>
  </Story>
</Preview>
