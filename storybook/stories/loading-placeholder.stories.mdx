import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { action } from "@storybook/addon-actions";
import { withKnobs, text, boolean, select } from "@storybook/addon-knobs";

import { LoadingPlaceholder } from "@rawpixel-public/react-components";

import "../.storybook/global.css";

<Meta title="Atoms/Loading Placeholder" decorators={[withKnobs]} />

# Loading Placeholder

Block with animated background which can be used as a placeholder while data is loading.

<Preview>
  <Story name="default">
    <LoadingPlaceholder />
  </Story>
</Preview>
